{
  "name": "document-locator",
  "version": "0.1.0",
  "private": true,
  "description": "CLI tool for crawling and searching documents (skeleton)",
  "main": "bin/document-locator",
  "bin": {
    "document-locator": "bin/document-locator"
  },
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}


{
  "name": "document-locator",
  "version": "0.1.0",
  "description": "Document locator CLI project scaffolded for Node.js 22 with pnpm.",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.20.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "crawler": "node --experimental-strip-types src/cli/crawler.ts",
    "search": "node --experimental-strip-types src/cli/search.ts",
    "db:apply": "node -e \"console.log('db:apply placeholder – no migrations defined yet');\"",
    "format": "biome format --write .",
    "lint": "biome check . --write",
    "test": "vitest run"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.6",
    "typescript": "^5.9.3",
    "vitest": "^4.0.10"
  }
}

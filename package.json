{
  "name": "document-locator",
  "version": "0.1.0",
  "private": true,
  "description": "CLI tool for crawling and searching documents (skeleton for Phase 1)",
  "bin": {
    "document-locator": "bin/document-locator"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test",
    "verify:supabase-files-upsert": "npm run build && node dist/scripts/verify-supabase-files-upsert.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.81.1",
    "commander": "^11.1.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
